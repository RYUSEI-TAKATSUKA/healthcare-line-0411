# Domain Layer

各ドメインは `spec/04_workflows/` に定義されたフローを実装するモジュールとして配置します。  
`goal-setting/`, `workout-planning/`, `workout-execution/`, `health-consultation/` など、ディレクトリ単位で独立した公開API・モデル・サービス・リポジトリを持たせます。

> ドメイン実装は今後のフェーズで追加します。
